[tool.poetry]
name = "dbt-demo-poetry"
version = "0.1.0"
description = ""
authors = ["Oliver Crawford <oliver@getorchestra.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
dbt-core = "^1.8.9"
dbt-duckdb = "^1.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
